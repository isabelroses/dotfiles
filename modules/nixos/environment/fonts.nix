{
  fonts = {
    fontconfig = {
      enable = true;
      hinting.enable = true;
      antialias = true;
    };

    # this can allow us to save some storage space
    fontDir.decompressFonts = true;
  };
}
